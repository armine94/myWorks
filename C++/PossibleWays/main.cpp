#include "countOfPossibleWayBuilding.h"

int main() {

    //Count of sections
    int n;

    entering(n);
    std::cout << "Count of ways for " << n << " sections is: " << countWays(n) << std::endl;

    entering(n);
    std::cout << "Count of ways for " << n << " sections is: " << countWays(n) << std::endl;

    entering(n);
    std::cout << "Count of ways for " << n << " sections is: " << countWaysFibo(n) << std::endl;

    return 0;
}
